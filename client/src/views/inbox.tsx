import { defineComponent } from 'vue';

export default defineComponent({
    name: 'InboxView',
    setup() {
        return () => (
            <div>
                INBOX
            </div>
        )
    }
})
