# Journal Entry - 2026-05-23

## Accomplishments
- **Database Schema & Migrations**:
  - Added SQLite FTS5 (Full Text Search 5) support to the `artists` table in `migrations/20260518161623_initial.sql`.
  - Defined a virtual table `artists_fts` with FTS5, declaring the UUID column (`id`) as `UNINDEXED` and the text fields (`name` and `sort_name`) as full-text indexed columns.
  - Implemented three synchronization triggers (`artists_after_insert`, `artists_after_delete`, and `artists_after_update`) to automatically propagate inserts, updates, and deletes from the main `artists` table to the FTS5 virtual table.

- **Proc-Macro Development**:
  - Updated the `SyncEntity` proc-macro generator in `src/derive/src/sync_entity/generator.rs` to automatically generate the newly introduced `search` method for the `SyncEntityRepository` trait.
  - The generated `search` function utilizes a compiled SQLite query that joins the target entity table with its `_fts` virtual table using the UUID `id` column, filters by active rows (`deleted = 0`), searches via `MATCH ?`, and orders by `f.rank` (relevance).

- **Testing & Verification**:
  - Enhanced the `test_repository_crud` unit test in `src/model/src/entities/artist.rs` to provision the FTS5 virtual table and synchronization triggers on the in-memory SQLite database, and added thorough assertions verifying searching behaves correctly before and after updates, soft deletes, and restores.
  - Updated the `sync_database` example in `src/db_sync/examples/sync_database.rs` to include the FTS5 schema and triggers for consistency.
  - Ran cargo checks, tests, and example execution, confirming that everything compiles and passes cleanly with 0 errors.

- **Journal Synchronization Idempotency**:
  - Implemented logic in `DsotDatabase::apply_journal` in `src/db_sync/src/database/entity_ops.rs` to deserialize the entity for incoming `SyncOperation::Create` operations and check if it already exists in the SQLite database.
  - If the entity ID already exists in the database, the execution of the SQL operation (`exec_op`) is ignored, while the journal entry is still committed to the redb transactional operation log. This prevents unique constraint violations during peer-to-peer log sync and conflict resolution.
  - Fixed a stringification bug in `src/derive/src/sync_entity/generator.rs` where `get_table_name` was wrapping the static table name in unnecessary double-quotes due to `stringify!(#table)`, changing it to `#table`.
  - Added a dedicated unit test `test_apply_journal_duplicate_create` in `src/model/src/entities/artist.rs` to verify that `apply_journal` correctly ignores duplicate `Create` operations while preserving journal logging and keeping the database consistent.

## Next Steps
- Implement support for querying search results at the service/business logic layer.
- Integrate full-text search with Iroh/Automerge sync components.
