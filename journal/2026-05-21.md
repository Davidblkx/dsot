# Journal Entry - 2026-05-21

## Accomplishments
- Fully implemented the missing CRUD logic in `TestRepository` inside `src/model/src/entities/artist.rs` (methods `insert`, `delete`, `restore`, `update`).
- Updated the database schema definition in `migrations/20260518161623_initial.sql` to define the `id` column as `BLOB` rather than `TEXT`. Since the table is defined as `STRICT` and the Rust models represent ID as `Uuid`, storing it as `BLOB` is required for SQLx to seamlessly serialize and deserialize the UUID binary representation without throwing constraint or length validation errors.
- Added `tokio` to `dsot_model` Cargo dependencies for testing.
- Implemented a robust async unit test `test_repository_crud` using `sqlx` in-memory SQLite (`sqlite::memory:`) database to verify all CRUD actions (`insert`, `get`, `list`, `update`, `delete`, `restore`) under `TestRepository` work exactly as expected.
- Corrected an issue where `sqlx::QueryBuilder::separated` was putting separators in the middle of dynamic assignment expressions (e.g. producing `column = , ?` syntax errors) by migrating `update` implementation to manual separator pushing.

## Next Steps
- Implement corresponding CRUD logic in other entity repositories as they are introduced.
- Build higher-level synchronization logic around the database repository using the Automerge/Iroh syncing traits.
